workspace creation

cd command to change directory
ls to list
mkdir to make directory

navigate to the respective directory using cd and ls commands
make directory there using mkdir
install colcon packages
use colcon build to builld the package
source the package using -  source install/setup.bash
run the ros2 architecture using - ros2 run {package name} [file name}
